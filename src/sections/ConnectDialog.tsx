import { SettingsDialog, type SettingsDialogProps } from "@/sections/settings/SettingsDialog";

export { SettingsDialog };

export function ConnectDialog(props: SettingsDialogProps) {
  return <SettingsDialog {...props} />;
}
